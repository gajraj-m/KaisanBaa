<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kaisanbaa">

    <uses-feature android:name="android.hardware.camera.any" />

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KaisanBaa"
        android:usesCleartextTraffic="true">
        <activity android:name=".Activities.ClickViewActivity"></activity>
        <activity android:name=".Activities.CameraActivity" />
        <activity android:name=".Activities.CameraApplication" />
        <activity android:name=".Activities.SettingsActivity" />
        <activity android:name=".Activities.ImageViewActivity" />
        <activity android:name=".Activities.GroupChatActivity" />
        <activity android:name=".Activities.ChatActivity" />
        <activity android:name=".Activities.SetupProfileActivity" />
        <activity android:name=".Activities.OTPActivity" />
        <activity android:name=".Activities.PhoneNumberActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>